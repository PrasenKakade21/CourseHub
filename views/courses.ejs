<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Courses</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .course-cards img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }
        .card-price {
            font-size: 1.2rem;
            font-weight: bold;
            color: #28a745;
        }
        .course-card {
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
            height: 100%;
        }
        .course-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }
        .courses-header {
            background-color: #007bff;
            color: white;
            padding: 3rem 0;
            margin-bottom: 2rem;
        }
        .card-footer {
            background-color: transparent;
            border-top: none;
        }
    </style>
</head>
<body>

    <%- include('navbar') %>

    <header class="courses-header">
        <div class="container text-center">
            <h1 class="display-4">Explore Our Courses</h1>
            <p class="lead">Discover a wide range of courses to boost your skills and advance your career</p>
        </div>
    </header>

    <section id="courses" class="py-5">
        <div class="container">
            <h2 class="text-center mb-5">Courses We Offer</h2>
            <div class="row g-4">
                <% if (Array.isArray(courses) && courses.length > 0) { %> 
                <% courses.forEach(function(course) { %> 
                  <%- include('course', { course:course }); %> 

                <% }); %>
                <% } else { %>
                <div class="col-12">
                    <div class="alert alert-info text-center" role="alert">
                        <i class="fas fa-info-circle me-2"></i>No courses are available at the moment. Please check back soon!
                    </div>
                </div>
                <% } %>
            </div>
        </div>
    </section>

    <section class="bg-light py-5">
        <div class="container">
            <h2 class="text-center mb-4">Why Learn with Us?</h2>
            <div class="row g-4">
                <div class="col-md-4">
                    <div class="text-center">
                        <i class="fas fa-graduation-cap fa-3x mb-3 text-primary"></i>
                        <h4>Quality Education</h4>
                        <p>Our courses are designed by industry experts to ensure you receive top-notch education.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="text-center">
                        <i class="fas fa-users fa-3x mb-3 text-primary"></i>
                        <h4>Community Support</h4>
                        <p>Join a vibrant community of learners and get support whenever you need it.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="text-center">
                        <i class="fas fa-medal fa-3x mb-3 text-primary"></i>
                        <h4>Recognized Certificates</h4>
                        <p>Earn certificates that are recognized and valued by employers worldwide.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <%- include('footer') %>

</body>
</html>