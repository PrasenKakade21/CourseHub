<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CourseHub - Master Your Skills</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
    <style>
        .hero-section {
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url("https://plus.unsplash.com/premium_photo-1677567996070-68fa4181775a?q=80&w=2072&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D") no-repeat center center/cover;
            height: 600px;
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .course-card {
            transition: transform 0.3s ease-in-out;
            height: 100%;
        }
        .course-card:hover {
            transform: translateY(-5px);
        }
        .course-card img {
            height: 200px;
            object-fit: cover;
        }
        .card-price {
            font-size: 1.2rem;
            font-weight: bold;
            color: #28a745;
        }
        .card-footer {
            background-color: transparent;
            border-top: none;
        }
    </style>
</head>
<body>
    <%- include('navbar') %>

    <section class="hero-section text-center">
        <div class="container">
            <h1 class="display-4 fw-bold mb-4">Master Your Skills with CourseHub</h1>
            <p class="lead mb-5">Explore a wide range of professional courses to advance your career and achieve your goals.</p>
            <a href="#courses" class="btn btn-primary btn-lg px-5 py-3">Explore Courses</a>
        </div>
    </section>

    <section id="courses" class="py-5">
        <div class="container">
            <h2 class="text-center mb-5">Featured Courses</h2>
            <div class="row g-4">
                <% if (Array.isArray(courses) && courses.length > 0) { %>
                    <% courses.forEach(function(course) { %>
                      <%= console.log(course) %>
                      <%- include('course', { course:course }); %> 
                      <% }); %>
                <% } else { %>
                    <div class="col-12">
                        <p class="text-center">No courses available at the moment. Check back soon!</p>
                    </div>
                <% } %>
            </div>
        </div>
    </section>

    <section class="bg-light py-5">
        <div class="container">
            <h2 class="text-center mb-5">Why Choose CourseHub?</h2>
            <div class="row g-4">
                <div class="col-md-4">
                    <div class="text-center">
                        <i class="fas fa-laptop-code fa-3x mb-3 text-primary"></i>
                        <h4>Expert Instructors</h4>
                        <p>Learn from industry professionals with years of experience.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="text-center">
                        <i class="fas fa-clock fa-3x mb-3 text-primary"></i>
                        <h4>Flexible Learning</h4>
                        <p>Study at your own pace, anytime and anywhere.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="text-center">
                        <i class="fas fa-certificate fa-3x mb-3 text-primary"></i>
                        <h4>Certification</h4>
                        <p>Earn recognized certificates upon course completion.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <%- include('footer') %>

    <script src="../functions.js"></script>
</body>
</html>